# Backend environment variables
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# Server
SERVER_PORT=8000

# Optional: where to store captured sessions (default: /data/sessions)
# CAPTURED_SESSIONS_DIR=/data/sessions
